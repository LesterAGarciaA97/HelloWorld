# Harbour
## Hello world in Harbour markup language

### Harbour is the open/free software implementation of a crossplatform, multi-threading, object-oriented, scriptable programming language, backwards compatible with xBase languages.

### It consists of a compiler and runtime libraries with multiple UI, database and I/O backends, its own build system and collection of libraries and bindings for popular APIs.